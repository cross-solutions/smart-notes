name: app_business

version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  app_common:
    path: "../app_common"
  app_data:
    path: "../app_data"
  app_util:
    path: "../app_util"
dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
  pedantic: 1.8.0+1
